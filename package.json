{
  "name": "barberherman",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "bump:cache": "node scripts/bump-cache-version.mjs",
    "test": "pnpm run test:static && pnpm run test:browser",
    "test:static": "node --check script.js && node --check scripts/bump-cache-version.mjs && node --test tests/static-audit.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.61.1"
  }
}
