{
  "rules": {
    ".read": false,
    ".write": false,
    "presence": {
      ".indexOn": ["seenAt"],
      ".read": "auth != null && query.orderByChild === 'seenAt' && ((query.startAt != null && query.limitToLast != null && query.limitToLast <= 500) || (query.endAt != null && query.limitToFirst != null && query.limitToFirst <= 25))",
      "$session": {
        ".read": "auth != null && $session === auth.uid",
        ".write": "auth != null && ($session === auth.uid || (!newData.exists() && data.child('seenAt').isNumber() && data.child('seenAt').val() < now - 150000))",
        ".validate": "newData.hasChildren(['seenAt'])",
        "seenAt": {
          ".validate": "newData.isNumber() && newData.val() >= now - 10000 && newData.val() <= now + 10000"
        },
        "$other": {
          ".validate": false
        }
      }
    }
  }
}
